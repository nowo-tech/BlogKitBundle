checks:
  php:
    code_rating: true
    duplication: true

filter:
  excluded_paths:
    - "demo/*"
    - "tests/*"
    - "vendor/*"
    - "node_modules/*"
    - "coverage*"
    - "src/Resources/public/*.js"

build:
  environment:
    php:
      version: 8.4
      ini:
        memory_limit: -1
        'xdebug.mode': coverage
    node:
      version: 20

  cache:
    directories:
      - vendor/
      - node_modules/

  tests:
    before:
      - composer validate --strict
      - composer install --no-interaction --prefer-dist --optimize-autoloader
    override:
      - command: composer test-coverage
        environment:
          XDEBUG_MODE: coverage
        coverage:
          file: coverage.xml
          format: clover
      - pnpm install --frozen-lockfile && pnpm run test:coverage
